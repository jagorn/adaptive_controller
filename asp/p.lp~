numero(1..3).
context(a;b;c;d).
value(0;1).
time(1..100).

#external _value(C,V,T): context(C), value(V), time(T).
#external _request(1..5,C,T): context(C), time(T).

#show numero/1.
#show _request/3.
#show _value/3.
